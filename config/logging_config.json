{
    "version": "1.00",
    "logging": {
        "max_files": 20,
        "max_lines_per_file": 500,
        "log_directory": "logs",
        "format": "%(asctime)s [%(levelname)s] %(name)s: %(message)s",
        "level": "INFO"
    }
}
